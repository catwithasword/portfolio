import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Panida Rumriankit - Software Engineer",
  description: "Portfolio of Panida Rumriankit, a Software Engineer specializing in Full-Stack Development with expertise in Python, JavaScript, TypeScript, and modern web frameworks.",
  keywords: "Software Engineer, Full Stack Developer, Python, JavaScript, TypeScript, Next.js, Django, React",
  authors: [{ name: "Panida Rumriankit" }],
  creator: "Panida Rumriankit",
  icons: {
    icon: [
      { url: "/favicon.ico", sizes: "any" },
      { url: "/icon.png", type: "image/png", sizes: "32x32" },
    ],
    shortcut: "/favicon.ico",
    apple: "/icon.png",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
